import { FilterAgeGroupPipe } from './filter-age-group.pipe';

describe('FilterAgeGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAgeGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
